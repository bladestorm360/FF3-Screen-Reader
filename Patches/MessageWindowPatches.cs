using System;
using HarmonyLib;
using MelonLoader;
using FFIII_ScreenReader.Core;

namespace FFIII_ScreenReader.Patches
{
    /// <summary>
    /// Minimal test patch - does nothing, just tests if patching works at all
    /// </summary>
    public static class MessageWindowPatches
    {
        // No patches - completely empty to test if mod loads
    }
}
